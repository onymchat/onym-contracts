[toolchain]
# Match plonk/prover/rust-toolchain.toml — onym-plonk-prover is a path
# dependency and the two crates share the arkworks-0.5 / jellyfish
# build graph.
channel = "1.88.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
